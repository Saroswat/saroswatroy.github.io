import type { Metadata } from "next";
import { experience } from "../../data/portfolio";

export const metadata: Metadata = { title: "Experience", description: "Saroswat Roy's progression from research and applied AI education to secure, reliable IT operations.", alternates: { canonical: "/experience" } };
export default function ExperiencePage() { return <div className="page-shell"><header className="page-hero"><p className="eyebrow">Experience</p><h1>Research depth. User context. Operational accountability.</h1><p className="lede">A progression from studying how software and language systems represent meaning to supporting technology where reliability, security and business continuity are daily concerns.</p></header><div className="timeline full">{experience.map((item, index) => <article key={item.role}><div className="timeline-marker"><span>{String(experience.length - index).padStart(2, "0")}</span></div><div className="timeline-period"><p>{item.period}</p><span>{item.location}</span></div><div className="timeline-body"><h2>{item.role}</h2><p className="timeline-org">{item.organisation}</p><p>{item.summary}</p><ul className="feature-list">{item.highlights.map(highlight => <li key={highlight}>{highlight}</li>)}</ul></div></article>)}</div><section className="ops-note"><p className="eyebrow">Why operations matter</p><h2>Production readiness is a product of lived constraints.</h2><p>IT operations provides direct experience of user impact, incident handling, security controls and the practical fragility of connected systems. That experience informs AI architecture: safe defaults, observable decisions, graceful failure and clear human ownership.</p></section></div>; }
