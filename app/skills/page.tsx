import type { Metadata } from "next";
import { skillGroups } from "../../data/portfolio";
export const metadata: Metadata = { title: "Skills", description: "Structured AI engineering, NLP, machine learning, responsible AI, development and operations capabilities.", alternates: { canonical: "/skills" } };
export default function SkillsPage() { return <div className="page-shell"><header className="page-hero"><p className="eyebrow">Capabilities</p><h1>A practical toolkit for trustworthy AI delivery.</h1><p className="lede">A working toolkit spanning agentic systems, language intelligence, machine learning, responsible AI, product engineering and technology operations.</p></header><div className="skills-grid">{skillGroups.map((group, index) => <article key={group.title}><span>0{index + 1}</span><h2>{group.title}</h2><p>{group.description}</p><ul>{group.skills.map(skill => <li key={skill}>{skill}</li>)}</ul></article>)}</div></div>; }
